name: Demo Expressions
on:
    push: 
    workflow_dispatch:
        inputs:
            toProd:
                type: boolean
                default: false
jobs:
    my-demo-job:
        runs-on: ubuntu-latest
        run: echo "Hello, World!"
